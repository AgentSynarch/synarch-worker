{
  "name": "synarch-worker",
  "version": "2.1.0",
  "description": "SYNARCH Worker Agent -- Production-grade task automation with circuit breakers, dependency ordering, retry logic, and real-time metrics.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node src/self-test.js"
  },
  "dependencies": {
    "node-cron": "^3.0.3",
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "chokidar": "^3.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
